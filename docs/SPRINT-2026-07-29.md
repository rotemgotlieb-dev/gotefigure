# W2 backend sprint log, 2026-07-29 evening

Running log, committed as work lands (wall-safety: nothing lives only in a session).
Lane: GoteFigure backend reviewable one-clicks. All work on branches; nothing merged,
nothing deployed, zero remote writes, provider pin stays `mock`.

## Scope item tiers (final)

| # | Item | Tier | Evidence |
|---|---|---|---|
| 1 | PR #13 merge-readiness | BUILT+VERIFIED | verify exit 0, **124/124** tests (6 files), catalog-lint PASS, dist-lint PASS, fresh worktree + npm ci, 2026-07-29 ~17:47 PT. Merge base == origin/main tip = zero-conflict merge. One-click doc: docs/PR13-MERGE-ONECLICK.md |
| 2 | Rebase storefront onto main | BUILT+VERIFIED | `feat/storefront-lights-up-rebased` created from the branch tip, `git rebase origin/main` replayed all 10 commits with zero conflicts; verify exit 0, 94/94 at the rebase point; original branch untouched |
| 3 | Two-shirt content cut | BUILT+VERIFIED (one sub-item OWNER-REVIEW-PENDING, below) | pieces.json = mushroom-tee + rabbit-tee at $40 (cited GoteFigure.md:27), CC1717 sizing + runs-small note, boolean soldOut; drop.json = window config, false live/50/31 deleted; catalog-lint + dist-lint PASS with reworked structural floors; verify exit 0, **124/124** |
| 4 | Pre-sale window mechanics | BUILT+VERIFIED | docs/PRESALE-WINDOW-DESIGN.md written BEFORE code; lib/commerce/window.ts pure state machine, 23 unit tests incl. copy-honesty pins (dollars never percent-off, no countdown language, no em dash); observed on the BUILT worker: authed /store renders the honest pre state, zero countdown markup, zero numeric scarcity |
| 5 | Part B seam wiring | BUILT+VERIFIED (FW checkout path PENDING-VERIFY) | store.astro / piece/[id].astro / SatchelDrawer consume lib/commerce; mock provider derives from pieces.json (catalog.mock.ts retired); seal routes by provider with a fail-loud FW bridge behind a dynamic import; pin stays mock, zero real-provider calls; dist-lint artifact-leak PASS (17 public artifacts clean). The FW checkout bridge cannot be observed until real products exist: PENDING-VERIFY by construction |
| 6 | /info 404 fix | BUILT+VERIFIED | live 404 confirmed (gotefigure.com/info = 404, 2026-07-29); fix/info-index adds info/index.astro; local build emits dist/client/info/index.html with the sketchbooks pointer + paperwork links; verify exit 0, 94/94 |
| 7 | Report-only set | REPORT-ONLY (as briefed) | docs/ASTRO-UPGRADE-ASSESSMENT.md (audit re-derived tonight: 10 vulns, 8 high; two-tier fix), docs/POST-CUTOVER-CLEANUP.md (9 items incl. a new copy-debt find), HANDOFF.md truth-synced (stale 7/10 story corrected with verified ancestry + live WebP check) |

## OWNER-REVIEW-PENDING (logged per the stop rule)

- **store-lights-up.astro's hardcoded 4-item drop array was NOT cut to 2.** Cutting it
  forces a floor-composition decision: the approved Direction A desktop floor is a
  `repeat(4, 1fr)` grid (store-lights-up.astro, the `.lu-floor` media block) so two pieces
  would sit lopsided in columns 1-2, and the phone flow would leave a near-empty second
  sub-slide. Any fix is a CSS/layout change, which the brief bans on the approved LOOK.
  Rotem's 7/18 round-2 worklist already schedules a redesign of exactly this region (split
  the shop into two scroll movements); the two-shirt floor belongs in that design pass,
  through the design workflow. The page stays a preview (not nav-linked, soft-lobbied,
  footer says "draft direction. nothing here is for sale yet."), and the LIVE store surface
  (store.astro) now tells the two-shirt truth.

## Honesty findings banked (beyond the brief)

1. The V3 store's notify/subscribe forms FAKED success and stored nothing (third instance
   of the email-loss class). Now wired to /api/subscribe (source 'drops') via the shared
   hardened helper. With the $35 waitlist promise, a fake waitlist form would have been a
   revenue-relevant silent failure at T-0.
2. store-ui.ts hardcoded the hero add-to-bag to piece id 'tee'; now data-driven.
3. Unverified promises removed from gated-store copy: "free US shipping", "30-day size
   swaps", drawer "free (US)" line. The policy decisions stay open vault items.
4. info/shipping.astro (PUBLIC, live) still carries POD-era shipping/returns promises that
   contradict the drop-1 model; flagged in POST-CUTOVER-CLEANUP.md item 9, not edited
   (policy wording is Rotem's).
5. One em dash was shipping in every rendered page (a Layout HTML comment); removed on the
   rebased branch.

## Verification ledger (fresh-context verifier: start here)

- PR #13 worktree: `npm ci && npm run verify` -> exit 0, 124/124 (branch 1d86bde).
- feat/storefront-lights-up-rebased tip: `npm run verify` -> exit 0, 124/124 (94 base + 23
  window + 7 net new/changed commerce+smoke). Built-worker observation: no-cookie /store =
  302; minted-cookie /store = 200 with both shirts, loud waitlist line, no countdown attrs,
  no counts; /piece/mushroom-tee = 200 with sizing table; old id /piece/tee = 302 /store.
  Gate cookie was minted with a LOCAL test signing key via .dev.vars (gitignored,
  check-ignore verified BEFORE the value landed).
- fix/info-index tip: `npm run verify` -> exit 0, 94/94; dist/client/info/index.html present.
- Guard: tonight's battery LANDED 17:39 PT (vault report), mid-lane: **VERDICT RED**, three
  live-side findings, none of them a defect in this lane's code and none contradicting it:
  (RED 1) the stale Vercel deployment publicly serves the pre-hardening store/vault incl.
  the og gallery bytes (cleanup doc item 8 upgraded to URGENT); (RED 2) the live Worker
  holds only TURNSTILE_SECRET_KEY, so GATE_CODE / GATE_SIGNING_KEY / FW_WEBHOOK_SECRET are
  all unset and remote D1 was never migrated (the one-click doc's step 2/3 now covers the
  gate secrets too); (RED 3) CI deploy has skipped on every run (merge != live, as this
  lane's HANDOFF sync already stated). Guard's closing note recommends exactly the rebase
  this lane shipped. Guard INFO worth carrying, CLOSED by this lane: the storefront lockfile
  delta rides the rebased branch, so it was audited tonight from the lockfile alone
  (npm audit --package-lock-only on the branch's package.json + lock). The dependency
  delta vs main is exactly ONE package, lenis ^1.3.25, and the audit result is identical
  to main's (10 vulns: 1 low, 1 moderate, 8 high; no new advisories from lenis).
